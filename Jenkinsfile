node('master') {
    stage ('Build') {
        checkout scm
        println('Building it!!!')
        sleep(3000)
    }
    stage ('Step2') {
        checkout scm
        println('Step2ing it!!!')
        sleep(3000)
    }
    stage ('Publish') {
        checkout scm
        println('Publishing it!!!')
        sleep(3000)
    }
}
