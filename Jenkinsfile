node('master') {
    stage ('Build') {
        checkout scm
        println('Building it!!!')
        sleep(5)
    }
    stage ('Step2') {
        checkout scm
        println('Step2ing it!!!')
        sleep(5)
    }
    stage ('Publish') {
        checkout scm
        println('Publishing it!!!')
        sleep(5)
    }
}
