node('master') {
    stage ('Build') {
        checkout scm
        println('Building it!!!')
        sleep(30000)
    }
    stage ('Step2') {
        checkout scm
        println('Step2ing it!!!')
        sleep(30000)
    }
    stage ('Publish') {
        checkout scm
        println('Publishing it!!!')
        sleep(30000)
    }
}
