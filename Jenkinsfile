node('master') {
    stage ('Build') {
        checkout scm
        println('Building it!!!')
        sleep(5)
    }
    stage ('Step2') {
        println('Step2ing it!!!')
        sleep(5)
    }
    stage ('Publish') {
        println('Publishing it!!!')
        sleep(5)
    }
}
